#include "main.h"

/**
 * _strcat - main function for concatenating two strings
 * @dest: destination string
 * @src: source string
 * Return: dest
 */
char *_strcat(char *dest, char *src)
{
	_strcat(dest, src);
	return (dest);
}
