#include "main.h"
#include <string.h>
/**
 * _strcat - Funtion  for concatenating  two strings
 * @dest: destination file
 * @src: source file
 * Return: dest
 */

char *_strcat(char *dest, char *src)
{
	_strcat(dest, src);

	return (dest);
}
